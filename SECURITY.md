# 安全策略

## 当前安全范围

电脑助手采用默认关闭、用户同意、允许列表和逐次确认的设计：

- 只接受本地绝对路径且真实存在的 `.exe`；
- 拒绝 UNC 网络路径、符号链接、脚本、快捷方式和参数；
- 使用直接进程创建，不调用 CMD、PowerShell 或系统 Shell；
- 拦截常见命令解释器、脚本运行时和高风险系统工具；
- 不设置 `runas`，要求管理员权限的程序应由 Windows 拒绝；
- 每次执行前显示解析后的完整路径并要求用户确认；
- 本地记录启动成功、失败和阻止结果，不记录聊天全文；
- 聊天中的任意文件路径不会直接成为执行目标，必须先与允许列表名称精确匹配。

开机启动只在用户主动勾选后写入当前用户的 `HKCU` Run 项，不申请管理员权限，也不创建系统服务或计划任务。启动命令只包含带引号的桌宠 EXE 完整路径和固定参数 `--autostart`。移动或删除程序文件夹前应先关闭该选项，避免留下失效启动项。

## 已知限制

允许列表不是恶意软件检测工具。合法文件可能被替换，已签名程序也可能存在漏洞；拦截清单也不可能覆盖所有可被滥用的系统程序。发布版本仍应结合代码签名、哈希或签名展示、自动化测试和安全软件扫描。

未来若接入 OpenClaw，默认必须使用本机回环连接、独立令牌、最小工具权限、执行询问和明确允许列表；不得默认开放远程频道、公共网络监听、任意 Shell 或无人确认的 `system.run`。

## 漏洞报告

正式发布前应在仓库中配置私密漏洞报告渠道，例如 GitHub Private Vulnerability Reporting 或专用安全邮箱。请不要在公开 Issue 中披露可直接利用的漏洞、令牌、API Key、个人路径或隐私数据。

建议报告包含：受影响版本、复现步骤、影响范围、最小化日志和建议修复。不要在未经授权的设备、账户或数据上进行安全测试。

## 发布前安全清单

- 完成依赖和第三方代码许可证审计；
- 对安装包进行恶意软件扫描并生成哈希；
- 使用可信代码签名证书签名；
- 验证无 API Key、令牌、个人路径或测试数据进入仓库；
- 测试普通用户权限、UAC 拒绝、路径替换、损坏配置和回滚；
- 发布明确的支持版本与漏洞修复策略。
